IO.puts rem 9,1
:thisisatom
byte_size("string")
byte_size("longerString")
byte_size(:atom)
byte_size(:longerAtom)
is_binary("Éoin")
IO.puts [1, 2, 3, 4] ++ [1,1,1,1]
add = fn a, b -> a + b end
IO.puts add.(1, 2)
